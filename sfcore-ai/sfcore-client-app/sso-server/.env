# Server Configuration
APP_ENV=development
APP_HOST=127.0.0.1
APP_PORT=8080
APP_NAME="SecureAuth SSO"

# Database Configuration (PostgreSQL)
# Format: postgres://user:password@host:port/database_name
DATABASE_URL=postgres://postgres:Password_Gua123!@localhost:5432/sfcore_sso
DATABASE_MAX_CONNECTIONS=10
DATABASE_MIN_CONNECTIONS=2

# Redis Configuration (Cache & Session)
# Format: redis://:password@host:port/db_index
REDIS_URL=redis://localhost:6379/0
REDIS_MAX_CONNECTIONS=10

# Security (JWT)
# Generate a secure random string (e.g., `openssl rand -hex 32`)
JWT_SECRET=5d41402abc4b2a76b9719d911017c592
JWT_ACCESS_TOKEN_EXPIRY=900      # 15 minutes in seconds
JWT_REFRESH_TOKEN_EXPIRY=604800  # 7 days in seconds

# Logging
RUST_LOG=info,sso_server=debug,tower_http=debug
