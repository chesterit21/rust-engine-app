# 📋 PLANNING TASK & SUB-TASK: DEPLOYMENT EXTENSION VS CODE

Berikut adalah planning task lengkap dari development hingga extension siap diinstall di VS Code.

---

## 🎯 OVERVIEW DEPLOYMENT PROCESS

```
Development → Testing → Build → Package → Distribution → Installation
     ↓           ↓         ↓        ↓           ↓             ↓
  Coding     QA Test   Webpack   .vsix    Marketplace    End User
```

---

## 📊 FASE DEPLOYMENT - DETAILED BREAKDOWN

### **PHASE 1: PROJECT INITIALIZATION & SETUP** ⏱️ 2-3 hours

#### Task 1.1: Environment Setup

- [ ] **1.1.1** Install Node.js v20+

  ```bash
  node --version  # Verify >= 20.0.0
  npm --version   # Verify >= 10.0.0
  ```

- [ ] **1.1.2** Install VS Code Extension Development Tools

  ```bash
  npm install -g @vscode/vsce      # Extension packaging tool
  npm install -g yo generator-code # Optional: scaffolding
  ```

- [ ] **1.1.3** Install Development Dependencies

  ```bash
  npm install --save-dev \
    @types/node@20 \
    @types/vscode@1.85 \
    @types/react@18 \
    @types/react-dom@18 \
    typescript@5.3 \
    webpack@5 \
    webpack-cli@5 \
    ts-loader@9 \
    eslint@8 \
    @typescript-eslint/parser@6 \
    @typescript-eslint/eslint-plugin@6
  ```

- [ ] **1.1.4** Install Runtime Dependencies

  ```bash
  npm install \
    react@18 \
    react-dom@18 \
    zustand@4 \
    @msgpack/msgpack@3
  ```

#### Task 1.2: Project Structure Creation

- [ ] **1.2.1** Create directory structure

  ```bash
  mkdir -p src/extension/{commands,providers,services,transport,utils}
  mkdir -p src/webview/{components,hooks,services,types,styles}
  mkdir -p src/shared
  mkdir -p media/{icons,images}
  mkdir -p test/{suite,fixtures}
  mkdir -p dist
  ```

- [ ] **1.2.2** Initialize Git repository

  ```bash
  git init
  echo "node_modules/\ndist/\n*.vsix\n.vscode-test/" > .gitignore
  ```

#### Task 1.3: Configuration Files Setup

- [ ] **1.3.1** Create `tsconfig.json`

  ```json
  {
    "compilerOptions": {
      "module": "commonjs",
      "target": "ES2020",
      "outDir": "./dist",
      "lib": ["ES2020"],
      "sourceMap": true,
      "rootDir": "./src",
      "strict": true,
      "esModuleInterop": true,
      "skipLibCheck": true,
      "forceConsistentCasingInFileNames": true,
      "resolveJsonModule": true
    },
    "exclude": ["node_modules", ".vscode-test"]
  }
  ```

- [ ] **1.3.2** Create `tsconfig.webview.json`

  ```json
  {
    "extends": "./tsconfig.json",
    "compilerOptions": {
      "jsx": "react",
      "lib": ["ES2020", "DOM"],
      "module": "ESNext",
      "moduleResolution": "node"
    },
    "include": ["src/webview/**/*"]
  }
  ```

- [ ] **1.3.3** Create `webpack.config.js` (refer to previous response)

- [ ] **1.3.4** Create `.eslintrc.json`

  ```json
  {
    "parser": "@typescript-eslint/parser",
    "extends": [
      "eslint:recommended",
      "plugin:@typescript-eslint/recommended"
    ],
    "rules": {
      "@typescript-eslint/no-explicit-any": "warn",
      "@typescript-eslint/explicit-module-boundary-types": "off"
    }
  }
  ```

- [ ] **1.3.5** Create `.vscodeignore`

  ```
  .vscode/**
  .vscode-test/**
  src/**
  test/**
  .gitignore
  .eslintrc.json
  tsconfig.json
  webpack.config.js
  **/*.map
  **/*.ts
  ```

---

### **PHASE 2: CORE DEVELOPMENT** ⏱️ 40-50 hours

#### Task 2.1: Transport Layer Implementation

- [ ] **2.1.1** Create transport type definitions

  ```typescript
  // src/extension/transport/types.ts
  ```

- [ ] **2.1.2** Implement UDS Transport

  ```typescript
  // src/extension/transport/udsTransport.ts
  // Test: npm run test:transport:uds
  ```

- [ ] **2.1.3** Implement HTTP Transport

  ```typescript
  // src/extension/transport/httpTransport.ts
  // Test: npm run test:transport:http
  ```

- [ ] **2.1.4** Create Transport Factory

  ```typescript
  // src/extension/transport/index.ts
  // Test: Auto-detection logic
  ```

- [ ] **2.1.5** Write unit tests for transport layer

  ```typescript
  // test/suite/transport.test.ts
  ```

#### Task 2.2: Extension Services

- [ ] **2.2.1** Implement Logger Utility

  ```typescript
  // src/extension/utils/logger.ts
  ```

- [ ] **2.2.2** Implement Config Manager

  ```typescript
  // src/extension/utils/config.ts
  ```

- [ ] **2.2.3** Implement LLM Service

  ```typescript
  // src/extension/services/llmService.ts
  ```

- [ ] **2.2.4** Implement Context Service

  ```typescript
  // src/extension/services/contextService.ts
  ```

- [ ] **2.2.5** Implement State Service

  ```typescript
  // src/extension/services/stateService.ts
  ```

#### Task 2.3: VS Code Integration

- [ ] **2.3.1** Create Webview Provider

  ```typescript
  // src/extension/providers/webviewProvider.ts
  ```

- [ ] **2.3.2** Implement Command Handlers

  ```typescript
  // src/extension/commands/chatCommands.ts
  // src/extension/commands/fileCommands.ts
  ```

- [ ] **2.3.3** Create Extension Entry Point

  ```typescript
  // src/extension/extension.ts
  ```

#### Task 2.4: React UI Development

- [ ] **2.4.1** Create VS Code API Bridge

  ```typescript
  // src/webview/services/vscodeApi.ts
  ```

- [ ] **2.4.2** Implement Custom Hooks

  ```typescript
  // src/webview/hooks/useChat.ts
  // src/webview/hooks/useFileContext.ts
  // src/webview/hooks/useVSCode.ts
  ```

- [ ] **2.4.3** Create UI Components

  ```typescript
  // src/webview/components/ChatPanel/index.tsx
  // src/webview/components/ChatPanel/ChatMessage.tsx
  // src/webview/components/ChatPanel/ChatInput.tsx
  // src/webview/components/FileContext/index.tsx
  // src/webview/components/ModeSelector/index.tsx
  ```

- [ ] **2.4.4** Create Main App Component

  ```typescript
  // src/webview/App.tsx
  ```

- [ ] **2.4.5** Create Entry Point

  ```typescript
  // src/webview/index.tsx
  ```

- [ ] **2.4.6** Styling

  ```css
  // src/webview/styles/global.css
  // src/webview/components/**/styles.css
  ```

#### Task 2.5: Shared Protocol

- [ ] **2.5.1** Define shared types and protocol

  ```typescript
  // src/shared/protocol.ts
  // src/shared/types.ts
  ```

---

### **PHASE 3: TESTING & QUALITY ASSURANCE** ⏱️ 8-12 hours

#### Task 3.1: Unit Testing

- [ ] **3.1.1** Setup test framework

  ```bash
  npm install --save-dev @types/mocha mocha
  ```

- [ ] **3.1.2** Write transport layer tests

  ```typescript
  // test/suite/transport.test.ts
  ```

- [ ] **3.1.3** Write service layer tests

  ```typescript
  // test/suite/services.test.ts
  ```

- [ ] **3.1.4** Write utility tests

  ```typescript
  // test/suite/utils.test.ts
  ```

- [ ] **3.1.5** Run all tests

  ```bash
  npm test
  ```

#### Task 3.2: Integration Testing

- [ ] **3.2.1** Test extension activation

  ```typescript
  // test/suite/extension.test.ts
  ```

- [ ] **3.2.2** Test webview communication

  ```typescript
  // test/suite/webview.test.ts
  ```

- [ ] **3.2.3** Test file context management

  ```typescript
  // test/suite/context.test.ts
  ```

- [ ] **3.2.4** Test LLM service integration

  ```typescript
  // test/suite/llm.test.ts
  ```

#### Task 3.3: Manual Testing

- [ ] **3.3.1** Test in Extension Development Host (F5)
  - Launch extension
  - Open chat panel
  - Send messages
  - Add file context
  - Switch modes
  - Test error scenarios

- [ ] **3.3.2** Test on different OS
  - [ ] Linux (UDS transport)
  - [ ] macOS (UDS transport)
  - [ ] Windows (HTTP fallback)

- [ ] **3.3.3** Performance testing
  - Test with large files (>1MB)
  - Test with multiple files (>10)
  - Test streaming performance
  - Memory leak check

---

### **PHASE 4: PACKAGE.JSON CONFIGURATION** ⏱️ 2-3 hours

#### Task 4.1: Basic Metadata

- [ ] **4.1.1** Configure package.json metadata

  ```json
  {
    "name": "ai-dev-agent",
    "displayName": "AI Dev Agent - Local LLM Assistant",
    "description": "AI-powered development assistant using local LLM server",
    "version": "0.1.0",
    "publisher": "your-publisher-name",
    "author": {
      "name": "Your Name",
      "email": "your.email@example.com"
    },
    "license": "MIT",
    "repository": {
      "type": "git",
      "url": "https://github.com/yourusername/ai-dev-agent"
    },
    "bugs": {
      "url": "https://github.com/yourusername/ai-dev-agent/issues"
    },
    "homepage": "https://github.com/yourusername/ai-dev-agent#readme",
    "keywords": [
      "ai", "llm", "assistant", "code-completion", 
      "local-ai", "development", "chat"
    ],
    "categories": [
      "AI", "Programming Languages", "Machine Learning"
    ],
    "engines": {
      "vscode": "^1.85.0"
    }
  }
  ```

#### Task 4.2: Extension Contributions

- [ ] **4.2.1** Configure activationEvents

  ```json
  "activationEvents": [
    "onStartupFinished"
  ]
  ```

- [ ] **4.2.2** Configure main entry point

  ```json
  "main": "./dist/extension.js"
  ```

- [ ] **4.2.3** Configure views and viewsContainers

  ```json
  "contributes": {
    "viewsContainers": {
      "activitybar": [{
        "id": "aiDevAgent",
        "title": "AI Dev Agent",
        "icon": "media/icons/agent.svg"
      }]
    },
    "views": {
      "aiDevAgent": [{
        "type": "webview",
        "id": "aiDevAgent.chatView",
        "name": "Chat",
        "icon": "media/icons/chat.svg"
      }]
    }
  }
  ```

- [ ] **4.2.4** Configure commands

  ```json
  "commands": [
    {
      "command": "aiDevAgent.openChat",
      "title": "Open AI Dev Agent Chat",
      "category": "AI Dev Agent"
    },
    {
      "command": "aiDevAgent.addFileToContext",
      "title": "Add File to AI Context",
      "category": "AI Dev Agent",
      "icon": "$(add)"
    },
    {
      "command": "aiDevAgent.clearContext",
      "title": "Clear AI Context",
      "category": "AI Dev Agent",
      "icon": "$(clear-all)"
    }
  ]
  ```

- [ ] **4.2.5** Configure menus

  ```json
  "menus": {
    "explorer/context": [{
      "command": "aiDevAgent.addFileToContext",
      "group": "aiDevAgent@1",
      "when": "!explorerResourceIsFolder"
    }],
    "editor/context": [{
      "command": "aiDevAgent.addFileToContext",
      "group": "aiDevAgent@1"
    }],
    "view/title": [{
      "command": "aiDevAgent.clearContext",
      "when": "view == aiDevAgent.chatView",
      "group": "navigation"
    }]
  }
  ```

- [ ] **4.2.6** Configure keybindings

  ```json
  "keybindings": [{
    "command": "aiDevAgent.openChat",
    "key": "ctrl+shift+a",
    "mac": "cmd+shift+a"
  }]
  ```

- [ ] **4.2.7** Configure configuration schema

  ```json
  "configuration": {
    "title": "AI Dev Agent",
    "properties": {
      "aiDevAgent.transport.type": {
        "type": "string",
        "enum": ["auto", "uds", "http"],
        "default": "auto",
        "description": "Transport protocol type",
        "enumDescriptions": [
          "Auto-detect (recommended)",
          "Unix Domain Socket (Linux/macOS)",
          "HTTP (Cross-platform)"
        ]
      },
      "aiDevAgent.transport.uds.socketPath": {
        "type": "string",
        "default": "/tmp/llm-server.sock",
        "description": "Unix Domain Socket file path"
      },
      "aiDevAgent.transport.http.baseUrl": {
        "type": "string",
        "default": "http://localhost:8080",
        "description": "HTTP server base URL"
      },
      "aiDevAgent.chat.streaming": {
        "type": "boolean",
        "default": true,
        "description": "Enable streaming responses"
      },
      "aiDevAgent.chat.maxContextFiles": {
        "type": "number",
        "default": 10,
        "description": "Maximum number of files in context"
      }
    }
  }
  ```

#### Task 4.3: Scripts Configuration

- [ ] **4.3.1** Add build scripts

  ```json
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "webpack --mode production",
    "watch": "webpack --mode development --watch",
    "compile": "tsc -p ./",
    "lint": "eslint src --ext ts,tsx",
    "lint:fix": "eslint src --ext ts,tsx --fix",
    "test": "node ./out/test/runTest.js",
    "test:unit": "mocha --require ts-node/register test/suite/**/*.test.ts",
    "package": "vsce package",
    "publish": "vsce publish"
  }
  ```

#### Task 4.4: Icon & Badge Assets

- [ ] **4.4.1** Create extension icon (128x128 PNG)

  ```json
  "icon": "media/icons/extension-icon.png"
  ```

- [ ] **4.4.2** Add badges to package.json

  ```json
  "badges": [{
    "url": "https://img.shields.io/badge/version-0.1.0-blue",
    "href": "https://github.com/yourusername/ai-dev-agent",
    "description": "Version"
  }]
  ```

- [ ] **4.4.3** Create gallery banner

  ```json
  "galleryBanner": {
    "color": "#1e1e1e",
    "theme": "dark"
  }
  ```

---

### **PHASE 5: BUILD & OPTIMIZATION** ⏱️ 4-6 hours

#### Task 5.1: Production Build

- [ ] **5.1.1** Clean previous builds

  ```bash
  rm -rf dist/
  ```

- [ ] **5.1.2** Run production build

  ```bash
  npm run build
  ```

- [ ] **5.1.3** Verify build output

  ```bash
  ls -lh dist/
  # Should see:
  # - extension.js
  # - extension.js.map
  # - webview.js
  # - webview.js.map
  # - webview.css
  ```

- [ ] **5.1.4** Check bundle sizes

  ```bash
  # extension.js should be < 500KB
  # webview.js should be < 1MB
  ```

#### Task 5.2: Code Optimization

- [ ] **5.2.1** Tree-shaking verification

  ```javascript
  // webpack.config.js
  optimization: {
    usedExports: true,
    sideEffects: false
  }
  ```

- [ ] **5.2.2** Minification check

  ```javascript
  // webpack.config.js - Production mode
  optimization: {
    minimize: true
  }
  ```

- [ ] **5.2.3** Code splitting (if needed)

  ```javascript
  optimization: {
    splitChunks: {
      chunks: 'all'
    }
  }
  ```

#### Task 5.3: Linting & Formatting

- [ ] **5.3.1** Run ESLint

  ```bash
  npm run lint
  ```

- [ ] **5.3.2** Auto-fix issues

  ```bash
  npm run lint:fix
  ```

- [ ] **5.3.3** Format code (optional: Prettier)

  ```bash
  npx prettier --write "src/**/*.{ts,tsx,css}"
  ```

---

### **PHASE 6: PACKAGING** ⏱️ 1-2 hours

#### Task 6.1: Pre-packaging Checklist

- [ ] **6.1.1** Verify all files included
  - Check `.vscodeignore` excludes dev files
  - Verify `dist/` contains all needed files
  - Ensure `media/` assets are present

- [ ] **6.1.2** Update CHANGELOG.md

  ```markdown
  # Changelog
  
  ## [0.1.0] - 2024-01-20
  ### Added
  - Initial release
  - Chat interface with local LLM
  - File context management
  - Search mode support
  - UDS and HTTP transport
  ```

- [ ] **6.1.3** Create/Update README.md

  ```markdown
  # AI Dev Agent
  
  ## Features
  - 🤖 Local LLM integration
  - 📁 File context management
  - 🔍 Search mode
  - ⚡ High-performance UDS transport
  
  ## Installation
  1. Download .vsix file
  2. Open VS Code
  3. Press Ctrl+Shift+P
  4. Select "Extensions: Install from VSIX..."
  
  ## Configuration
  ...
  ```

- [ ] **6.1.4** Verify LICENSE file exists

  ```bash
  # Create MIT license if not exists
  touch LICENSE
  ```

#### Task 6.2: Create VSIX Package

- [ ] **6.2.1** Install vsce (if not already)

  ```bash
  npm install -g @vscode/vsce
  ```

- [ ] **6.2.2** Verify package.json validity

  ```bash
  vsce ls
  # Should list all files to be packaged
  ```

- [ ] **6.2.3** Create package

  ```bash
  vsce package
  # Output: ai-dev-agent-0.1.0.vsix
  ```

- [ ] **6.2.4** Verify package contents

  ```bash
  unzip -l ai-dev-agent-0.1.0.vsix
  # Verify:
  # - extension/ folder
  # - package.json
  # - dist/ files
  # - media/ files
  # - README.md
  # - CHANGELOG.md
  ```

#### Task 6.3: Package Size Optimization

- [ ] **6.3.1** Check package size

  ```bash
  ls -lh ai-dev-agent-0.1.0.vsix
  # Target: < 5MB for optimal distribution
  ```

- [ ] **6.3.2** Optimize if needed
  - Remove source maps in production
  - Compress images
  - Review .vscodeignore

---

### **PHASE 7: LOCAL TESTING** ⏱️ 2-4 hours

#### Task 7.1: Installation Testing

- [ ] **7.1.1** Install from VSIX

  ```bash
  # Method 1: Command line
  code --install-extension ai-dev-agent-0.1.0.vsix
  
  # Method 2: VS Code UI
  # Ctrl+Shift+P → "Extensions: Install from VSIX..."
  ```

- [ ] **7.1.2** Verify installation

  ```bash
  code --list-extensions | grep ai-dev-agent
  ```

- [ ] **7.1.3** Check extension appears in sidebar
  - Look for AI Dev Agent icon in activity bar
  - Click to open chat panel

#### Task 7.2: Functional Testing

- [ ] **7.2.1** Test activation
  - Extension should activate on VS Code startup
  - No errors in Output → Extension Host

- [ ] **7.2.2** Test chat functionality
  - [ ] Send message
  - [ ] Receive response
  - [ ] Streaming works
  - [ ] Error handling

- [ ] **7.2.3** Test file context
  - [ ] Add file from explorer (right-click)
  - [ ] Add file from editor (right-click)
  - [ ] File appears as @filename chip
  - [ ] Remove file from context
  - [ ] Clear all context

- [ ] **7.2.4** Test mode switching
  - [ ] Normal mode
  - [ ] Search mode
  - [ ] Correct flag sent to server

- [ ] **7.2.5** Test commands
  - [ ] Ctrl+Shift+A opens chat
  - [ ] All commands in command palette work

- [ ] **7.2.6** Test configuration
  - [ ] Change transport type
  - [ ] Change socket path
  - [ ] Settings persist

#### Task 7.3: Cross-platform Testing

- [ ] **7.3.1** Test on Linux
  - UDS transport works
  - File paths correct
  - UI renders properly

- [ ] **7.3.2** Test on macOS
  - UDS transport works
  - Keybindings work (Cmd instead of Ctrl)

- [ ] **7.3.3** Test on Windows
  - HTTP fallback works
  - File paths with backslashes work
  - UI renders properly

#### Task 7.4: Performance Testing

- [ ] **7.4.1** Startup time
  - Extension activation < 500ms
  - No blocking on startup

- [ ] **7.4.2** Memory usage
  - Baseline < 50MB
  - After 10 messages < 100MB
  - No memory leaks

- [ ] **7.4.3** Response time
  - UDS latency < 100ms
  - HTTP latency < 500ms

---

### **PHASE 8: DOCUMENTATION** ⏱️ 3-4 hours

#### Task 8.1: User Documentation

- [ ] **8.1.1** Create comprehensive README.md

  ```markdown
  # AI Dev Agent
  
  ## 📖 Overview
  ...
  
  ## ✨ Features
  ...
  
  ## 📦 Installation
  ...
  
  ## 🚀 Quick Start
  ...
  
  ## ⚙️ Configuration
  ...
  
  ## 🔧 Server Setup
  ...
  
  ## 📝 Usage Examples
  ...
  
  ## 🐛 Troubleshooting
  ...
  
  ## 🤝 Contributing
  ...
  
  ## 📄 License
  ...
  ```

- [ ] **8.1.2** Create USAGE.md with screenshots
  - Add screenshots of UI
  - Add GIF demos of features
  - Step-by-step guides

- [ ] **8.1.3** Create FAQ.md

  ```markdown
  # Frequently Asked Questions
  
  ## Q: How do I connect to my local LLM?
  A: ...
  
  ## Q: Why is UDS not working on Windows?
  A: ...
  ```

#### Task 8.2: Developer Documentation

- [ ] **8.2.1** Create CONTRIBUTING.md

  ```markdown
  # Contributing Guide
  
  ## Development Setup
  ...
  
  ## Building
  ...
  
  ## Testing
  ...
  
  ## Pull Request Process
  ...
  ```

- [ ] **8.2.2** Create ARCHITECTURE.md

  ```markdown
  # Architecture Documentation
  
  ## System Overview
  ...
  
  ## Component Diagram
  ...
  
  ## Data Flow
  ...
  
  ## API Reference
  ...
  ```

- [ ] **8.2.3** Add JSDoc comments to all public APIs

#### Task 8.3: Server Integration Guide

- [ ] **8.3.1** Create SERVER_SETUP.md

  ```markdown
  # Server Setup Guide
  
  ## Rust Server Configuration
  ...
  
  ## UDS Socket Setup
  ...
  
  ## HTTP Endpoint Setup
  ...
  
  ## Protocol Specification
  ...
  ```

---

### **PHASE 9: DISTRIBUTION** ⏱️ 2-3 hours

#### Task 9.1: GitHub Release (Manual Distribution)

- [ ] **9.1.1** Create GitHub repository

  ```bash
  git remote add origin https://github.com/yourusername/ai-dev-agent.git
  git push -u origin main
  ```

- [ ] **9.1.2** Create release tag

  ```bash
  git tag -a v0.1.0 -m "Initial release"
  git push origin v0.1.0
  ```

- [ ] **9.1.3** Create GitHub Release
  - Go to Releases → Draft a new release
  - Title: "v0.1.0 - Initial Release"
  - Description: Copy from CHANGELOG.md
  - Upload: `ai-dev-agent-0.1.0.vsix`

- [ ] **9.1.4** Add installation instructions to release notes

#### Task 9.2: VS Code Marketplace (Optional)

- [ ] **9.2.1** Create Azure DevOps account
  - Go to <https://dev.azure.com>
  - Create organization

- [ ] **9.2.2** Create Personal Access Token
  - User Settings → Personal Access Tokens
  - Scope: Marketplace (Manage)
  - Copy token securely

- [ ] **9.2.3** Create publisher

  ```bash
  vsce create-publisher your-publisher-name
  ```

- [ ] **9.2.4** Login to publisher

  ```bash
  vsce login your-publisher-name
  # Enter PAT when prompted
  ```

- [ ] **9.2.5** Publish to marketplace

  ```bash
  vsce publish
  # Or specify version
  vsce publish 0.1.0
  ```

- [ ] **9.2.6** Verify on marketplace
  - Go to <https://marketplace.visualstudio.com>
  - Search for your extension
  - Verify all info displays correctly

#### Task 9.3: Alternative Distribution

- [ ] **9.3.1** Share via direct download
  - Host .vsix on GitHub releases
  - Host on company server
  - Share via file sharing

- [ ] **9.3.2** Create installation script

  ```bash
  #!/bin/bash
  # install.sh
  code --install-extension ai-dev-agent-0.1.0.vsix
  ```

---

### **PHASE 10: POST-DEPLOYMENT** ⏱️ Ongoing

#### Task 10.1: Monitoring

- [ ] **10.1.1** Monitor GitHub issues
- [ ] **10.1.2** Monitor VS Code marketplace reviews (if published)
- [ ] **10.1.3** Check error logs from users
- [ ] **10.1.4** Track download statistics

#### Task 10.2: User Support

- [ ] **10.2.1** Create support channels
  - GitHub Discussions
  - Issue templates
  - Email support

- [ ] **10.2.2** Create troubleshooting guide
- [ ] **10.2.3** Maintain FAQ with common issues

#### Task 10.3: Updates & Maintenance

- [ ] **10.3.1** Plan update cycle
  - Bug fixes: Weekly
  - Features: Monthly
  - Major versions: Quarterly

- [ ] **10.3.2** Version bump workflow

  ```bash
  # Bump version
  npm version patch  # 0.1.0 → 0.1.1
  npm version minor  # 0.1.1 → 0.2.0
  npm version major  # 0.2.0 → 1.0.0
  
  # Rebuild
  npm run build
  
  # Repackage
  vsce package
  
  # Republish
  vsce publish
  ```

---

## 🎯 QUICK REFERENCE: DEPLOYMENT COMMANDS

### **Development**

```bash
# Watch mode
npm run watch

# Run in Extension Development Host
# Press F5 in VS Code
```

### **Testing**

```bash
# Run all tests
npm test

# Run specific test
npm run test:unit
```

### **Building**

```bash
# Clean build
rm -rf dist/
npm run build
```

### **Packaging**

```bash
# Create VSIX
vsce package

# List package contents
vsce ls
```

### **Installation**

```bash
# Install locally
code --install-extension ai-dev-agent-0.1.0.vsix

# Uninstall
code --uninstall-extension your-publisher.ai-dev-agent
```

### **Publishing**

```bash
# Publish to marketplace
vsce publish

# Publish specific version
vsce publish 0.1.1
```

---

## 📊 CHECKLIST RINGKASAN

### Pre-Deployment ✅

- [ ] All code written and tested
- [ ] No TypeScript errors
- [ ] All tests passing
- [ ] Documentation complete
- [ ] CHANGELOG updated
- [ ] Version bumped

### Packaging ✅

- [ ] Production build successful
- [ ] VSIX created
- [ ] Package size < 5MB
- [ ] All assets included

### Testing ✅

- [ ] Installed from VSIX
- [ ] All features work
- [ ] No console errors
- [ ] Tested on target platforms

### Distribution ✅

- [ ] GitHub release created
- [ ] VSIX uploaded
- [ ] Installation instructions clear
- [ ] (Optional) Published to marketplace

### Post-Deployment ✅

- [ ] Support channels active
- [ ] Monitoring setup
- [ ] Update plan in place

---

## 🚨 TROUBLESHOOTING DEPLOYMENT

### Common Issues & Solutions

**Issue 1: "vsce: command not found"**

```bash
npm install -g @vscode/vsce
```

**Issue 2: "Publisher not found"**

```bash
vsce create-publisher your-publisher-name
vsce login your-publisher-name
```

**Issue 3: "Package too large"**

```bash
# Check .vscodeignore
# Remove source maps in production
# Optimize webpack bundle
```

**Issue 4: "Extension not activating"**

```bash
# Check package.json activationEvents
# Verify main entry point exists
# Check extension.ts exports activate()
```

**Issue 5: "Webview not loading"**

```bash
# Verify webpack bundled webview.js
# Check webview.asWebviewUri() paths
# Enable CSP in webview HTML
```

---

## 📈 SUCCESS METRICS

| Metric | Target |
|--------|--------|
| **Build Time** | < 30s |
| **Package Size** | < 5MB |
| **Activation Time** | < 500ms |
| **Test Coverage** | > 70% |
| **Installation Success** | 100% on target platforms |

---

Dengan mengikuti planning task ini secara sistematis, Anda akan memiliki extension VS Code yang:

- ✅ **Professional**: Mengikuti best practices
- ✅ **Tested**: Quality assurance lengkap
- ✅ **Documented**: User & developer docs
- ✅ **Distributable**: Siap di-deploy ke production
- ✅ **Maintainable**: Mudah di-update

Silakan mulai dari **PHASE 1** dan lanjutkan step-by-step. Semoga sukses! 🚀
